'use client'

import TransactionTable from "./TransactionTable";

const Transaction = () => {
    return (
        <div>
            <h2>Transaction</h2>
            <TransactionTable/>
        </div>
    );
};

export default Transaction;